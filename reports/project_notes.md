# Project notes

This repository was prepared for portfolio/GitHub presentation. The code is
organized so the full experiment can be re-run from the raw Crunchbase-derived
CSV, while the current `artifacts/` folder preserves the outputs used in the
final report.

The most important modeling decision is the strict-vs-expanded feature split:
expanded features improve performance, but strict features are more realistic
for early-stage forecasting.
